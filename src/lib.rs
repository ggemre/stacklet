pub mod external {
    pub mod exec;
    pub mod model;
    pub mod widget;
}
pub mod utils {
    pub mod args;
    pub mod filter;
    pub mod helpers;
}
pub mod interface {
    pub mod window;
}
use crate::external::widget::Widget;
// use crate::interface::window::BreakCondition;
// use std::process::exit;
